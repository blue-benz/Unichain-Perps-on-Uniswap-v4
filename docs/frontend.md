# Frontend

Path: `/frontend`

Features in terminal UI:
- Open/modify long/short form.
- Funding display.
- Liquidation monitor panel.
- LP hedge comparison panel (unhedged vs hedged drawdown).

Build:
```bash
npm install
npm run frontend:dev
npm run frontend:build
```

Shared ABIs are consumed from `/shared/abi`.
